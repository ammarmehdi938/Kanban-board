import { colors, createTheme } from "@mui/material";

const listTheme = createTheme({
  palette: {
    primary: {
      main: "#252525",
      secondary: "red",
    },
  },
});

export default listTheme;
